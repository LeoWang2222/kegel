# 凯格尔运动小助手 · 精修版交接

当前版本：5.1（2026-09-16）

- 成品目录：桌面「凯格尔运动小助手·精修版」。参考目录保持原样。
- 仓库：https://github.com/LeoWang2222/kegel
- Pages：https://leowang2222.github.io/kegel/
- 部署：main 分支根目录；无运行时第三方依赖。修改 app.mjs/core.mjs 后执行 npm run build，生成普通脚本 app.js，以支持双击 index.html。npm run check 会检查生成文件是否同步。
- 文件和使用方法见 README.md。

## 不要破坏的行为

1. localStorage 键仍为 kegel_helper_v1，日期为不补零的本地 年-月-日。
2. 首次旧数据迁移备份到 kegel_helper_v1_pre_v5；保留旧 holdSec、目标、累计组数与 checkins。旧历史组数不完整时只显示历史达标，不伪造详细次数。
3. core.mjs 的 Session 只计算时序，app.mjs 使用 requestAnimationFrame 供时；松手在帧间到达截止时间也应计入。
4. 按住模式必须实际松手才进入放松；自动模式按计时交替，不检测发力。两种模式最终一次放松结束后才完成一组。自动模式后台不补算次数。
5. pendingSession 在每次阶段变化后持久化，内含独立 ID、模式、次数和该组原先的时长。刷新/关闭后恢复时先准备 3 秒，未完成收紧重做，放松计足。completedSessions 保存最近 100 个组 ID，用于避免同组重复计入；记录与检查点清除在同一次存储写入中完成。
6. 训练中与完成小结页不自动刷新应用。下载更新后，在离开本次训练与小结后切换版本。
7. 每次修改应用资源必须递增 sw.js 的 CACHE。缓存只删除 kegel- 前缀的旧版本，避免影响同源其他应用。
8. 字体为系统字体；SVG 吉祥物、图标不依赖外部 CDN。只在关键阶段发出可关闭的轻提示音，不承诺 iPhone 网页能提供原生触觉反馈。
9. 清空记录需要应用内二次确认，仅清空本应用记录，保留当前训练节奏。
10. 用户偏好：中文，功能克制，重视精美画面和按压手感；在独立桌面文件夹交付，并上传 GitHub 可直接访问。
11. 用户已明确要求视觉升级不要顺带修改收紧时间、放松时间和每日目标。trainingMode 默认 hold，theme 默认 system，新字段不改变原有参数。
12. 深色主题与 5.1 交互样式位于 refinements.css。主题、模式和进度仅保存本机。另一标签更新共享状态时，本页终止活动计时并回首页，避免同时训练。

## 验证

npm test（21 项）与 npm run check 均通过。没有连接真实 iPhone，不能把浏览器检查称作真机验证。进一步手感迭代请使用真实设备反馈。
